# Инструкция для агента-помощника: параллельная обработка кадров Dog FACS

Этот документ — для **агента Claude другого участника команды**, у которого есть
локальный GPU. Задача: помочь обработать часть датасета параллельно с основной
машиной, **не обрабатывая повторно** то, что уже сделано.

Прочитай целиком, затем выполняй по шагам. Все команды — из корня репозитория.

---

## 1. Что вообще происходит

Проект **Dog FACS** (Politechnika Gdańska, WETI) — датасет эмоций собак в формате COCO.
Люди вручную разметили в видео **момент начала и конца эмоции** (2 таймкода на видео).
Скрипт обработки для каждого размеченного видео:

1. берёт кадр в секунде «начало» и в секунде «конец»,
2. прогоняет через модели: **bbox (YOLOv8) → keypoints (46 точек HRNet) → порода (EfficientNet) → AU (21 Action Unit, дельта относительно нейтрального кадра)**,
3. эмоция берётся из ручной разметки,
4. пишет COCO: `annotations.json` + по классам + кадры (jpg) + `licenses.csv` + `frames.csv`.

Скрипт обработки: **`scripts/annotation/build_colab.py`** (несмотря на имя, отлично
работает и локально на GPU — просто читает видео из локальных папок).

---

## 2. Что обрабатывать и как не дублировать

**Ты обрабатываешь ВСЕ эмоции**: `neutral`, `sad`, `happy`, `surprise`, `angry`, `fearful`.
У тебя быстрый GPU, поэтому весь датасет считаешь локально в свою папку `release_participant/`.

От двойной обработки защищают:
1. **Чекпоинт `done_keys`** — внутри твоего прогона уже обработанные видео пропускаются.
   Можно прерывать (Ctrl+C, выключение) и запускать ту же команду снова — продолжит с места.
   Чекпоинт лежит в твоей выходной папке `release_participant/_checkpoint.json`.
2. **Мердж с дедупом** — если основная команда параллельно что-то посчитала на Colab,
   финальный `merge_releases.py` объединит всё и выкинет дубли по `source_video`
   (побеждает вариант с посчитанным AU). Так что пересечения не страшны.

Итог твоей работы — папка `release_participant/`, которую отдашь основной команде.

---

## 3. Предусловия (это настраивает человек, не агент)

Тебе от участника нужны две вещи с Google Drive (агент сам их не достанет):
доступ к общему фолдеру `DOGS` (там и видео, и веса) и — для пути A — файлы доступа
Drive API. Подробно про видео и два пути их получения — сразу ниже.

Если нет доступа к фолдеру `DOGS` — пусть участник попросит владельца (Маша) расшарить.

### Где лежат ВСЕ видео (полный список — в репозитории)

**Все размеченные видео лежат в одном общем фолдере Google Drive** — `DOGS`
(ID `1jxUaN3Mq1ge8lFcPzwnN2ISl0E9k9mfQ`). Отдельных папок «по эмоциям» искать не надо:
эмоция берётся из метки, а не из имени папки.

Полный перечень всех **1025** размеченных видео (только те, что реально надо
обрабатывать — с таймкодами эмоции) — в файле репозитория **`docs/video_manifest.csv`**
(колонки `emotion, video, folder, fid, drive_link`). Это и есть «карта» где что лежит.
Файл пересобирается командой `PYTHONPATH=. python -m scripts.annotation.build_manifest`.

| эмоция | видео | из них с известной папкой |
|--------|-------|---------------------------|
| angry    | 193 | — |
| sad      | 189 | — |
| surprise | 173 | — |
| happy    | 166 | — |
| neutral  | 162 | — |
| fearful  | 142 | 142 (`dog_fearful_anton` 89 + `dog_fearful_anton_2` 53) |

- **Все 1025 видео** имеют прямой Drive file ID (`fid`) → колонка `drive_link` даёт ссылку
  `https://drive.google.com/uc?id=<fid>`, качается напрямую (`gdown <fid>` или Drive API).
  Это значит: для запуска обработки достаточно `fid` из манифеста, папку знать не обязательно.
- **812 видео** имеют ещё и известную папку-источник на Drive (колонка `folder`) — удобно,
  если качаешь папками (путь B). У остальных 213 (старые метки) папку определить нельзя,
  но прямой `fid`-линк работает.

Тебе нужен **доступ к общему фолдеру `DOGS`** (расшаривает Маша) и веса моделей
(4 файла, ~393 МБ: `yolov8m.pt`, `breed.pt`, `keypoints_dogflw.pt`, `dogface_yolo.pt` —
лежат на Drive в `DOGS/dogfacs_colab/`). Метки качать не нужно — они в git.

### Как достать видео — два пути

**Путь A (рекомендуется, без ручного поиска папок): Drive API.**
Возьми у основной команды `secrets/gdrive_credentials.json` + `secrets/token.json`,
положи в `secrets/`, и запусти `build_150plus.py` (см. раздел 5, вариант A). Он сам
скачивает каждое видео из `DOGS` по `fid`, а для видео без `fid` — находит по имени.
Ничего локально заранее качать не надо.

**Путь B (офлайн, локальные папки): `build_colab.py`.**
Если Drive API настраивать не хочешь — синхронизируй локально **весь** фолдер `DOGS`
целиком (`rclone` или Google Drive для десктопа) в одну директорию, например
`~/dogfacs_videos/`. Скрипт ищет каждое видео по имени во всех указанных папках, поэтому
важно скачать всё — пропущенная папка = часть видео уйдёт в счётчик `brak_w_indeksie`.
Служебную `dogfacs_colab/` и папки результатов `release_*` в видео-директорию класть не нужно.

---

## 4. Установка окружения

```bash
# 1. код + метки разметки (репозиторий публичный)
git clone https://github.com/eternaki/group-project.git
cd group-project

# 2. зависимости (Python 3.10+). Если есть CUDA — поставь torch под свою версию CUDA
pip install torch torchvision ultralytics timm opencv-python numpy pycocotools

# 3. веса моделей — положи 4 файла в ./models/
mkdir -p models
# скопируй сюда yolov8m.pt, breed.pt, keypoints_dogflw.pt, dogface_yolo.pt
ls models/   # должно быть 4 .pt файла
```

Проверь, что видит GPU:
```bash
python -c "import torch; print('CUDA:', torch.cuda.is_available())"
```
Должно быть `CUDA: True`. Если `False` — обработка пойдёт на CPU (медленнее, но тоже
сработает; скрипт сам это определит).

---

## 5. Запуск обработки

### Вариант A — Drive API (без ручного скачивания папок)

Если положил `secrets/gdrive_credentials.json` + `secrets/token.json` (взять у команды) и
дал доступ к фолдеру `DOGS`, просто запусти основной скрипт — он сам тянет видео с Drive
по `fid`/имени, обрабатывает **все эмоции** и пишет в `data/dataset_final/release_150plus/`:

```bash
pip install google-api-python-client google-auth google-auth-oauthlib   # для Drive API
PYTHONPATH=. python -m scripts.annotation.build_150plus         # можно --limit N для пробы
```

Это тот же пайплайн, что гоняет основная команда. Первый запуск попросит авторизоваться
в браузере (обновит `secrets/token.json`). Результат отдаёшь так же, как в разделе 6
(папку переименуй в `release_participant/`, чтобы не путать с их прогоном).

### Вариант B — локальные папки (офлайн, без Drive API)

Скачал весь `DOGS` локально (см. раздел 3, путь B) — управляй через переменные окружения:

```bash
# все скачанные подпапки с видео разом — каждая ~/dogfacs_videos/*/ попадёт в поиск
export COLAB_VIDEO_DIRS="$(ls -d $HOME/dogfacs_videos/*/ | tr '\n' ';')"
export COLAB_LABELS_DIR="data/labels/dataset_final"
export COLAB_WORK="release_participant"          # рабочая папка (тут копится результат)
export COLAB_OUTPUT="release_participant"         # финальная папка = та же, локально
export COLAB_DEVICE="cuda"                         # сам упадёт на cpu, если GPU нет
export COLAB_EMOTIONS="neutral,sad,happy,surprise,angry,fearful"   # все эмоции
export PYTHONPATH="."

python -m scripts.annotation.build_colab
```

Что увидишь:
- `Etykiet (angry,fearful,happy,neutral,sad,surprise): 1296 | plików wideo w folderach: M`
- загрузку моделей, `Modele załadowane (cuda)`
- прогресс каждые 10 видео: `... 50/1296 | kadrów 90 | {'ok': 45, ...}`
- чекпоинт пишется каждые 50 видео в `release_participant/_checkpoint.json`

**Сколько меток должно найтись (это в git, у всех одинаково).** Строка `Etykiet` покажет
общее число ~**1296**. По эмоциям (сколько видео с этой меткой существует):

| эмоция | меток |
|--------|-------|
| angry    | ~193 |
| sad      | ~189 |
| surprise | ~173 |
| happy    | ~166 |
| neutral  | ~162 |
| fearful  | ~142 |

Если по какой-то эмоции обрабатывается **заметно меньше** (много `brak_w_indeksie`) —
значит не хватает папок с видео: докачай недостающие подпапки `DOGS/` и запусти снова
(уже сделанное пропустится по чекпоинту).

**Можно прерывать в любой момент** (Ctrl+C, выключение). При повторном запуске той же
команды продолжит с чекпоинта — уже обработанные пропустятся.

### Что значат счётчики
- `ok` — успех, полные данные (bbox+keypoints+порода+AU+эмоция)
- `brak_psa` — модель не нашла собаку в кадре (вернуть нельзя, битых данных нет)
- `brak_neutralnej` — нет нейтрального кадра → нельзя посчитать AU (видео пропущено)
- `brak_odczytu` — файл не читается/битый
- `brak_w_indeksie` — видео из метки нет в скачанных папках (проверь, что скачал все папки из таблицы)

> Замечание про AV1: часть роликов (`o8zoo…`) закодированы в AV1. На локальном Linux с
> нормальным ffmpeg (`ffmpeg -decoders | grep av1` показывает `libdav1d`) они обработаются.
> Если декодера нет — попадут в `brak_odczytu`, тогда доставь `ffmpeg` с av1.

---

## 6. Результат и передача

По завершении в `release_participant/` будет:
- `annotations.json` + по одному `annotations_<эмоция>.json` на каждую эмоцию
- папки `neutral/`, `sad/`, `happy/`, `surprise/`, `angry/`, `fearful/` с кадрами (jpg)
- `licenses.csv`, `frames.csv`

**Отдай всю папку `release_participant/`** основной команде — загрузи её на общий Drive
(например в `DOGS/dogfacs_colab/release_participant/`) или передай архивом.

Основная команда объединит твой результат со своим одной командой:
```bash
python -m scripts.annotation.merge_releases release_final release_colab release_participant
```
`merge_releases.py` склеит оба в `release_final/`, убрав дубли по `source_video`
(при дубле побеждает вариант с посчитанным AU). Перенумерует id и соберёт единый COCO.

---

## 7. Если что-то идёт не так

- **Много `brak_w_indeksie`** — скачаны не все папки с видео из `DOGS/` (раздел 3), или
  `COLAB_VIDEO_DIRS` собрался пустым. Проверь `echo $COLAB_VIDEO_DIRS` и что в
  `~/dogfacs_videos/` действительно лежат подпапки с видео. Докачай недостающее и запусти
  снова — сделанное пропустится по чекпоинту.
- **`Modele załadowane (cpu)` вместо cuda** — torch не видит GPU. Проверь установку CUDA-сборки
  torch. На CPU тоже сработает, просто медленнее (~1 видео/сек против ~7/сек на GPU).
- **`brak_neutralnej` у многих** — норма для сложных видео (нет спокойного кадра пса);
  такие видео пропускаются, это ожидаемо.
- **Скрипт упал на середине** — просто запусти ту же команду снова, продолжит с чекпоинта.

Ты считаешь **все эмоции**. Если основная команда параллельно посчитает что-то на Colab —
не страшно: финальный `merge_releases.py` объединит оба результата и выкинет дубли по
`source_video` (см. раздел 6).
